// tailwind.config.ts
import type { Config } from "tailwindcss";

const config: Config = {
    theme: {
        extend: {
            fontFamily: {serif: ["Merriweather", "sans"],
            },
        },
    },
    plugins: [],
};

export default config;